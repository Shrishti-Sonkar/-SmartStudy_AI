# Example env file — do NOT commit real secrets
# Frontend (Vite):
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Supabase function / server side:
SUPABASE_URL=
SERVICE_ROLE_KEY=
GROQ_API_KEY=

# Local dev only (not for production):
# Create a separate .env.local and add to .gitignore
